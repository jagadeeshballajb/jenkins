pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'ls -l'

            }
        }
        stage ('test') {
            steps {
                sh 'free -m'

            }
        }
        stage ('deploy') {
            steps {
                sh 'uptime'

            }
        }
    }
}
