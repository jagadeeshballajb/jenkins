pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'ls -l'

            }
        }
        stage ('test') {
            steps {
                sh 'pwd'

            }
        }
        stage ('deploy') {
            steps {
                sh 'uptime'

            }
        }
    }
}
