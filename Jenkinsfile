pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'ls -l'

            }
        }
        stage ('test') {
            steps {
                echo 'worked'

            }
        }
        stage ('deploy') {
            steps {
                sh 'uptime'

            }
        }
    }
}
