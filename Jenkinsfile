pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'ls -l'

            }
        }
        stage ('test') {
            steps {
                sh 'whoami'

            }
        }
        stage ('deploy') {
            steps {
                sh 'top'

            }
        }
    }
}
