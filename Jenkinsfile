pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'ls -l'

            }
        }
        stage ('test') {
            steps {
                echo 'Testing first pipeline'

            }
        }
        stage ('deploy') {
            steps {
                echo 'Deploying first pipeline'

            }
        }
    }
}
