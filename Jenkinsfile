pipeline{
    agent any
    stages {
        stage ('Build') {
            steps {
                echo 'Building first pipeline'

            }
        }
        stage ('test') {
            steps {
                echo 'Testing first pipeline'

            }
        }
        stage ('deploy') {
            steps {
                echo 'Deploying first pipeline'

            }
        }
    }
}
